{
  "name": "gui2one_web_components",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle --format=esm --outdir=./build ./components/index.ts",
    "watch": "esbuild --bundle --format=esm --watch --outdir=./build ./components/index.ts",
    "gen": "npm run build && node ./node/generate_bundle.mjs && esbuild ./build/g2_bundle.js --minify --outfile=./build/g2_bundle.min.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild-plugin-tsc": "^0.4.0",
    "lit-html": "^3.1.0",
    "ts-node": "^10.9.2",
    "tsc-watch": "^6.0.4",
    "typescript": "^5.3.3"
  }
}
